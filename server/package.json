{
  "name": "nasa_backend_project",
  "version": "1.0.0",
  "description": "learing fullstack node project",
  "main": "bin",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "nodemon src/server.js",
    "startSetPort": "set PORT=5000 && node src/server.js",
    "start": "node src/server.js"
  },
  "author": "vikash parida",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
